# Policy storage at /data/adb/isolationpolicy is intentionally left in place
# on uninstall, in case the module is reinstalled later and the user wants
# their denylist back. Remove it manually if you want a clean uninstall:
#   rm -rf /data/adb/isolationpolicy
